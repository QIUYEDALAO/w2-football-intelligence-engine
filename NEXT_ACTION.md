# NEXT ACTION

```text
ACTIVE_NEXT_ACTION = W2_MI_R3_MARKET_RADAR_AND_MODEL_LAB
OWNER_AUTHORIZATION = W2_MI_R3_MARKET_RADAR_MODEL_LAB_20260808
ROUND_1 = PASS
ROUND_2 = PASS_WITH_TERMINAL_PROVIDER_PLAN_RESTRICTION
FREE_PLAN_BRIDGE_CONTROLLED_RUNTIME = PASS
ROUND_3 = AUTHORIZED_IN_PROGRESS
```

## Binding read order

```text
1. CURRENT_STATE.yaml
2. NEXT_ACTION.md
3. ROUND_3_OWNER_AUTHORIZATION.md
4. ROUND_3_CODEX_EXECUTION.md
5. ROUND_3_ACCEPTANCE_CRITERIA.md
6. FREE_PLAN_BRIDGE_CONTROLLED_RUNTIME_RECEIPT.md
7. CURRENT_PRODUCT_DESIGN.md
8. REPOSITORY_HYGIENE_POLICY.md
```

## Goal

Continuously execute the complete Round-3 Market Radar / Model Lab objective from latest trusted `origin/main` until every hard acceptance gate passes.

```text
live code/evidence audit
-> evidence eligibility
-> same-line market timeline
-> Market Radar facts/movement
-> Model Lab diagnostic consensus comparison
-> isolate legacy edge semantics from public authority
-> seven-state projection
-> API/read model
-> existing public Web surfaces
-> tests / CI / bounded remediation
-> merge
-> immutable W2 staging deployment
-> real persisted-evidence acceptance
-> rollback
-> repository hygiene
-> ROUND_3_FINAL_RECEIPT.md
```

Do not stop after intermediate steps to request owner approval. Inside the authorized scope:

```text
FAIL_CLOSED = STOP_AT_FAILED_GATE -> DIAGNOSE -> MINIMAL_FIX -> RERUN -> CONTINUE
```

## Core semantic boundary

Market Radar reports market facts. Model Lab reports diagnostics.

```text
MOVEMENT != OPPORTUNITY
ANOMALY != OPPORTUNITY
MODEL_MARKET_DIVERGENCE != MARKET_OPPORTUNITY
BETTING_EDGE_CLAIM = FORBIDDEN
```

Round-3 public authority must not depend on legacy EV/cashflow-edge/analysis-direction fields.

Model-market disagreement requires ready same-line evidence, fresh quotes, >=3 bookmakers and model probability outside the observed real bookmaker de-vig probability range.

Do not invent a statistical anomaly threshold. `NOT_CALIBRATED` is a valid Round-3 outcome.

## Runtime invariants

```text
R3_AUTHORIZATION_MAIN_SHA = c241b877a4168659f465163108f7a53fb8fd82a5
FREE_BRIDGE_MODE = SHADOW_ONLY
API_FOOTBALL_PLAN = FREE
PROVIDER_DAILY_LIMIT = 100
W2_DAILY_CALL_CEILING = 80
MIN_PROVIDER_DAILY_REMAINING = 20
AUTOMATIC_RETRY = false
ACTIVE_WHITELIST = EXACT_EXISTING_13
AUDIT_ONLY_RUNTIME_REACHABILITY = 0
```

Development/CI manual Provider calls = 0. Dashboard/API reads must never trigger Provider calls.

## Stop lines

Do not buy/renew Provider access, change the active whitelist, enable the four audit-only leagues, reopen Phase 0.5/H, create recommendation/value/opportunity semantics, enable Candidate/Formal/Lock/Production, or execute real-money behavior.

Detailed execution authority: `ROUND_3_CODEX_EXECUTION.md`.

Binding hard gates: `ROUND_3_ACCEPTANCE_CRITERIA.md`.
