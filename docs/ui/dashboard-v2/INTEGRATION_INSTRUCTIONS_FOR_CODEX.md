# Dashboard V2 Historical Evidence Notice

This pack is retained only as historical visual evidence and as a transitive dependency of the protected Boss-console acceptance fixture.

It is not an integration instruction, public route, fallback, product authority or future implementation plan. The only public dashboard authority is `NEW_INTELLIGENCE_WORKSPACE_ONLY`, rendered by `DashboardPage -> IntelligenceConsole` from `GET /v1/dashboard/intelligence-workspace`.

Do not route, import or rebuild Dashboard V2 as a public product surface.
