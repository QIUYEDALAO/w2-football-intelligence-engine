# W2 Strategy Correlation V1

Secondary directions require a calibrated correlation policy. Until correlation
thresholds are validated, secondary output is suppressed with
`CORRELATION_POLICY_INSUFFICIENT`.

This prevents highly related directions from being presented as independent
research signals.
