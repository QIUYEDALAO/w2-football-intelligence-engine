# W2 Daily Operations V1

Daily checks cover scheduler, worker, API health, upcoming fixture coverage,
odds freshness, bookmaker coverage, quota, mapping conflicts, T-24h/T-1h locks,
result sync, backup freshness, unresolved alerts, and forward holdout progress.

Recommendation-facing checks are `DISABLED_GATE4` until Gate 4 is closed.
