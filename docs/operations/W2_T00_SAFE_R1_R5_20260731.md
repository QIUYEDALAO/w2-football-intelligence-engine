# W2 T00-SAFE Wave 1 grouped-review-queue finalization report — 2026-08-01

## Boundary and reproduction

```text
EXACT_BASE_SHA = dbc8e1e8aa74a7613fd7121bf6026890c3ee06c6
SCAN_STRATEGY = ALL_PRODUCTION_AST_CALLS_WITH_EXACTLY_ONE_DISPOSITION
PR450_REVIEW_HEAD = 360931d7d84bcbe1416c7946992b5218b759fc8a
```

```bash
python3 scripts/audit_t00.py safe --remote github-w2 \
  --pr450-ref refs/remotes/github-w2/pull/450/head --compact
```

The current-repository compact SAFE output SHA-256 is
`c033aaaad5d8d1e22a579acffd8e52d7e59841b3dea0eacb5cba0338b01012c9`.
This report proposes classifications and tests; it records no independent
acceptance and implements no runtime change.

## Production Python scan scope

The scanner derives all tracked Python roots from the exact base and fails if
any discovered root has neither an included-production disposition nor an
explicit exclusion reason. The production denominator includes deployed app
composition roots and the complete migration runtime, not only revisions.

```text
INCLUDED_PRODUCTION_ROOTS = apps/, migrations/, scripts/, src/w2/
EXCLUDED_PYTHON_ROOTS = tests/: TEST_ONLY_NOT_PART_OF_PRODUCTION_DENOMINATOR
UNCLASSIFIED_PYTHON_ROOTS = 0
```

The exact base contains seven `apps/**/*.py` files; both `apps/api/main.py` and
`apps/worker/celery_app.py` are scanned. A contract fixture adds an unknown
production root and requires `UNCLASSIFIED_PYTHON_ROOTS` hard failure.

## Complete R2 handler denominator

R2 enumerates every `ast.ExceptHandler` in the exact-base Python tree, including
handlers that appear to terminate. Its control-flow walk excludes raises inside
nested functions/classes/lambdas, and its conservative all-path test distinguishes
conditional raise plus fallthrough from terminal raise.

```text
R2_HANDLER_DENOMINATOR = 449
ALL_PATHS_HANDLER_CONTROL_FLOW_TERMINATES = 386
HANDLER_MAY_FALLTHROUGH = 63
```

| handler action | count |
|---|---:|
| `RAISE_TERMINAL` | 83 |
| `CONDITIONAL_RAISE_WITH_FALLTHROUGH` | 8 |
| `RETURN` | 257 |
| `CONTINUE` | 43 |
| `PASS` | 6 |
| `ROLLBACK_THEN_CONTINUE` | 4 |
| `DIAGNOSTIC_THEN_CONTINUE` | 6 |
| `CALL_ONLY_THEN_CONTINUE` | 4 |
| `STATE_UPDATE_THEN_CONTINUE` | 38 |

These 449 rows are a denominator, not 449 defects. Contract tests freeze
conditional-raise/fallthrough and nested-function-raise behavior.

## All-production-call universe and R3 denominator

Every `ast.Call` under the included roots receives exactly one disposition.
No `_io_call()` miss or other pre-filter can remove a call from the universe.
Internal calls bind an exact project definition and callee candidate ID; pure
or local transforms require an exact signature in the reviewed allowlist.

```text
TOTAL_PRODUCTION_AST_CALLS = 42054
INTERNAL_PROJECT_CALLS = 10241
KNOWN_PURE_OR_LOCAL_TRANSFORMS = 9590
RECOGNIZED_SIDE_EFFECT_PRIMITIVES = 710
UNRESOLVED_EXTERNAL_CALLS = 6868
UNRESOLVED_DYNAMIC_CALLS = 14645
UNACCOUNTED_PRODUCTION_CALLS = 0
```

The invariant is `42054 = 10241 + 9590 + 710 + 6868 + 14645`.
Unresolved external and dynamic calls remain in the frozen completeness ledger;
they are not treated as pure or silently removed. The ledger-derived R3 boundary
remains:

```text
R3_SIDE_EFFECT_DENOMINATOR =
  RECOGNIZED_SIDE_EFFECT_PRIMITIVES
+ UNRESOLVED_EXTERNAL_CALLS
+ UNRESOLVED_DYNAMIC_CALLS
= 22223
R3_SIDE_EFFECT_DENOMINATOR = 22223
```

The explicit side-effect registry covers Provider/network transport, DB,
Redis/KV, file/lock/durability, process execution, object store/message, and
the trusted-base `shutil` writes. Exact regressions include:

```text
src/w2/providers/api_football.py@dbc8e1e8...:139
urllib.request.urlopen -> NETWORK_TRANSPORT
src/w2/data_assets/registry.py@dbc8e1e8...:193
shutil.copy2 -> FILE_COPY
src/w2/data_assets/registry.py@dbc8e1e8...:196
shutil.copytree -> FILE_TREE_COPY
```

```text
REDIS_KV_PRIMITIVES = 2
LOCK_DURABILITY_PRIMITIVES = 13
PROCESS_EXECUTION_PRIMITIVES = 24
```

| operation | count |
|---|---:|
| `NETWORK_TRANSPORT` | 75 |
| `DB_COMMIT` | 49 |
| `DB_EXECUTE` | 30 |
| `DB_FLUSH` | 16 |
| `DB_WRITE_STAGE` | 49 |
| `DB_ROLLBACK` | 31 |
| `DB_READ` | 23 |
| `FILE_READ` | 267 |
| `FILE_WRITE` | 118 |
| `FILE_REPLACE` | 8 |
| `FILE_COPY` | 1 |
| `FILE_TREE_COPY` | 1 |
| `FILE_TREE_DELETE` | 3 |
| `FILE_LOCK_OPERATION` | 9 |
| `FILE_DURABILITY_SYNC` | 4 |
| `REDIS_KV_WRITE` | 1 |
| `REDIS_KV_SCRIPT_WRITE` | 1 |
| `PROCESS_EXECUTION` | 24 |

Exact-site regressions freeze `redis_client.set` at line 344,
`redis_client.eval` at line 389, `fcntl.flock` at line 351, and `os.fsync` at
line 375 of `src/w2/ingestion/future_refresh.py`, plus production subprocess
sites. Synthetic unknown external and dynamic calls freeze both unresolved
classes, and an invalid sixth disposition hard-fails the accounting validator.

## Separate grouped decision queue

The scanner builds a static call-edge manifest rooted at
`scripts/run_prematch_refresh.py:main`. It resolves imports, same-module calls,
`self` calls, and constructor-method calls, and records two explicit dynamic
dispatch proposals for the default Provider client and request ledger. The
full scan records 42,054 call sites plus two reviewed dynamic edge proposals;
993 rooted edges cover 132 proposed reachable functions.

The 42,054-row ledger is now a separate artifact from the human review queue.
The queue groups unresolved external calls by exact external signature, domain
and caller context; unresolved dynamic calls by receiver shape, method, domain
and rooted reachability. Recognized side effects and R2 handlers use equivalent
stable operation/handler keys. Every one of the 22,672 R2/R3 review members is
assigned to exactly one group.

```text
ALL_CALL_COMPLETENESS_LEDGER = FROZEN_UNCHANGED
ALL_CALL_LEDGER_UNCHANGED = true
GROUPED_REVIEW_QUEUE = GENERATED_PENDING_INDEPENDENT_REVIEW
REVIEW_EXCEPTIONS = EXPLICIT_PENDING_INDEPENDENT_REVIEW
TOTAL_REVIEW_MEMBERS = 22672
TOTAL_REVIEW_GROUPS = 5671
CANARY_REVIEW_GROUPS = 1156
DEFERRED_REVIEW_GROUPS = 4515
REVIEW_EXCEPTION_SITES = 2245
UNASSIGNED_REVIEW_GROUP_MEMBERS = 0
UNGROUPED_UNRESOLVED_CALLS = 0
DUPLICATE_REVIEW_GROUP_MEMBERS = 0
```

One-shot canary groups require machine evidence: rooted recognized side effect,
rooted unresolved frontier, exact C1-C11 mapping, reviewed side-effect method,
or an explicit Provider/business-write/evidence-persistence boundary. Remaining
groups retain deterministic domain dispositions (`PROPOSED_DEFERRED_GATE_B/C/D`
or `PROPOSED_ACCEPTED_WITH_REASON`) and remain visible in the ledger.

```text
PROPOSED_GATE_A_GROUPS = 28
FINAL_GATE_A_GROUPS = 0
MAPPED_TO_C1_C11 = 35
NEW_FINDING_IDS = 0
INDEPENDENT_REVIEW_PENDING_GROUPS = 5671
INDEPENDENTLY_ACCEPTED_GROUPS = 0
```

The exact R2 candidate at `src/w2/ingestion/future_refresh.py:2098` maps only
to C6. C10 remains a separate historical validated finding scoped to
`FOREGROUND_ISOLATION_COMPOSE_RESTART_NO`; this correction neither removes nor
downgrades C10 and does not alter any denominator, group membership, or count.

The 2,245 explicit exceptions comprise exact blocker mappings and complex
dynamic receiver sites that cannot be proven solely by their group rule. Every
group and exception retains `PENDING_INDEPENDENT_REVIEW`; S07.8 remains open.

## Proposed test contracts

The 30 former Gate-A templates are now `PROPOSED_TEST_CONTRACT`. Each includes
an exact base path/line failure stage and per-delta evidence. Only checkpoint
claim validation has source evidence for provider/business deltas of zero.
Other provider, business, or durable-evidence deltas remain `PENDING_REVIEW`
where prior calls/writes or unimplemented fault injection prevent an exact
claim. No uniform `1/0/1` template remains.

```text
PROPOSED_TEST_CONTRACTS = 30
INDEPENDENTLY_ACCEPTED_TEST_CONTRACTS = 0
INCOMPLETE_PROPOSED_TEST_CONTRACTS = 0
```

## `3420714d` evidence state

```text
SCRIPT_MATRIX_ROWS = 145
SCRIPT_MATRIX_COLUMNS = 9
SCRIPT_MATRIX_CELL_UNIVERSE = 1305
CURRENTLY_ACCOUNTED_FIELDS = 1160
ROLE_FIELDS_CARRIED_TO_PR450 = 145
UNASSIGNED_FIELDS = 0
SCRIPT_MATRIX_FIELDS = 1160
SCRIPT_MATRIX_EVIDENCE_ATTACHED = 1160
INDEPENDENTLY_VERIFIED_FIELDS = 0
PENDING_INDEPENDENT_REVIEW_FIELDS = 1160
```

Evidence attachment is not semantic verification. Codex does not sign G10.7.
All 145 `role` cells have disposition
`CARRY_TO_PR450_DOCUMENTATION_REPAIR` and remain outside the eight current
field-rule groups. The column-accounting guard requires `9 = 8 + 1` and fails
if any future matrix column is neither audited nor explicitly carried forward.

The G10.7 review bundle groups all fields by one of eight field-specific
verification algorithms and lists every field not completely proven by that
algorithm as an exception:

```text
FIELD_REVIEW_TOTAL = 1160
FIELD_REVIEW_RULE_GROUPS = 8
FIELD_REVIEW_RULE_COVERED = 547
FIELD_REVIEW_EXCEPTIONS = 613
UNASSIGNED_FIELDS = 0
INDEPENDENTLY_VERIFIED_FIELDS = 0
```

## Corrected PR #450 guard matrix

The scanner parses the trusted main, PR #450 head, and current PR #458 head
only through exact Git objects. It never reads the unchecked delivery-test
worktree. PR #458 does not modify that file and claims no restoration.

```text
PR450_EXACT_EQUIVALENTS = 15
LOST_IN_PR450 = 145
RETAINED_ON_TRUSTED_MAIN = 145
PR450_REPAIR_REQUIRED_GUARDS = 145
UNCLASSIFIED_REMOVED_GUARDS = 0
```

Repair remains on PR #450 via its own rebase/commit. No guard was deleted,
skipped, xfailed, or relaxed by PR #458.

## Other inventories

```text
R1_CONFIGURATION_CANDIDATES = 84
R4_CONCURRENCY_CANDIDATES = 43
R5_COMPUTATION_SYMBOLS = 287
TOTAL_CLASSIFIED_OR_GROUPED_AST_CANDIDATES = 23086
UNRESOLVED_EXTERNAL_AND_DYNAMIC_LEDGER_SITES = 21513
UNGROUPED_UNRESOLVED_CALLS = 0
UNCLASSIFIED_COMPUTATION_AUTHORITIES = 0
```

The 21,513 unresolved sites remain exact external/dynamic ledger members and
are fully assigned to review groups; they are not 21,513 individual Gate
decisions. Computation authorities remain split into 76 explicit concept/hash domains.
Migration inventory still includes `table.create/table.drop`; metadata coupling
remains Gate D and Gate A retains only manual migration-head preflight.

## Validation boundary

Portable `github-w2`, missing-ref, R2 control flow, R3 primitive, candidate-edge,
Ruff, Mypy, unit/contract, credential-pattern scan, and exact-head path-aware-CI results are
recorded in PR #458 and Issues #454/#455 after the report commit.

```text
FULL_CI = NOT_RUN
REAL_PROVIDER_CALL_EXECUTED = false
REAL_CANARY_AUTHORIZATION_CREATED = false
AUTO_MERGE_EXECUTED = false
```

The report byte hash is stored in the adjacent `.sha256` sidecar.
