# W2 T00-SAFE corrected R1–R5 report — 2026-07-31

## Boundary and reproduction

```text
EXACT_BASE_SHA = dbc8e1e8aa74a7613fd7121bf6026890c3ee06c6
SCAN_STRATEGY = AST_FIRST_WITH_TEXT_FALLBACK
PR450_REVIEW_HEAD = 360931d7d84bcbe1416c7946992b5218b759fc8a
```

```bash
python3 scripts/audit_t00.py safe --remote github-w2 \
  --pr450-ref refs/remotes/github-w2/pull/450/head --compact
```

The scanner reads source from the exact base tree. Remote, main ref, PR ref,
PR head, and base SHA are configurable. The corrected compact SAFE output
SHA-256 is
`b07c16f98d8ed09bcbb1904c5bfdbc591222efd3a8c3fe1086b4f8eee1cd6d0e`.

## R1–R5 candidate closure

Every emitted candidate now contains:

```text
path, line, risk_family, classification, owner,
required_test, status, target_gate
```

The zero count is derived from all AST candidates, not from the separate
C1–C11 identifier list.

| family | candidates | disposition boundary |
|---|---:|---|
| R1 configuration/defaults | 67 | reviewed config boundary |
| R2 error/rollback | 76 | open error-boundary finding |
| R3 transaction/external I/O | 146 | open I/O/transaction finding |
| R4 concurrency/idempotency | 43 | reviewed concurrency authority |
| R5 computation/hash domains | 286 | domain-scoped authority or version boundary |

```text
TOTAL_CLASSIFIED_AST_CANDIDATES = 618
UNCLASSIFIED_FINDINGS = 0
```

`OPEN_FINDING` is a classified status, not a claim that runtime remediation is
complete. PR #458 implements no C1–C11 runtime change.

## Computation authority domains

The 286 symbols are grouped into 76 explicit business concept/hash domains.
Metric, odds/taxonomy, and settlement/probability functions remain distinct
business computation concepts. Hash/canonical functions are bounded by their
migration file, offline script, or `src/w2` source module. Each group records a
version/input boundary; function-name substrings alone never require all hashes
to converge on one serializer.

```text
UNCLASSIFIED_COMPUTATION_AUTHORITIES = 0
CANONICAL_SERIALIZER_IMPLEMENTED = false
HASH_DOMAINS_COLLAPSED = false
```

## Storage and migration inventory

The exact-base inventory contains 59 ORM table authorities and no duplicate
`__tablename__`. Migration call sites include both Alembic operations and
dynamic SQLAlchemy table operations:

```text
op.create_table = 58
op.drop_table = 37
op.rename_table = 0
table.create = 16
table.drop = 15
TRACKED_RUNTIME_OR_REPORT_ASSETS = 0
```

`R5_MIGRATION_METADATA_COUPLING` remains `MUST_FIX_FOR_GATE_D`. Gate A retains
only `GATE_A_MIGRATION_HEAD_PREFLIGHT = MANUAL_PREFLIGHT_ONLY`; this report does
not reclassify Gate D work into Gate A.

## Corrected PR #450 guard matrix

The baseline contains 160 test/assertion guards:

```text
PR450_EXACT_EQUIVALENTS = 15
PR450_REMOVED_GUARDS = 145
LOST_AND_RESTORED = 145
INTENTIONALLY_RETIRED_WITH_EVIDENCE = 0
UNCLASSIFIED_REMOVED_GUARDS = 0
```

Each exact equivalent points to the actual PR #450 test name and line at exact
head `360931d7...`. A semantic equivalent is accepted only through the explicit
`SEMANTIC_GUARD_REVIEWS` mapping and a verified target assertion. A retirement
requires `RETIRED_GUARD_REVIEWS` evidence. Otherwise a missing item is
`UNCLASSIFIED` unless a concrete PR #458 test/assertion restores the normalized
guard. The 145 omissions have such concrete targets and are therefore
`LOST_AND_RESTORED`, never automatically `RETAINED_EQUIVALENT` merely because
trusted main still contains them.

No historical guard was deleted, skipped, xfailed, or relaxed.

## Portable and failure-path validation

An isolated repository using only remote name `github-w2` reproduced both GOV
and SAFE. A second isolated repository without the PR object exited cleanly and
printed:

```text
git fetch github-w2 refs/pull/450/head:refs/remotes/github-w2/pull/450/head
```

No raw Git/Python traceback was emitted.

## Correct CI evidence for PR #458

```text
PATH_AWARE_CI = SUCCESS
python-focused = 1939 passed
images = SUCCESS / images built and pushed / no deployment
CI_REQUIRED = SUCCESS
verify = SKIPPED
predeploy-e2e = SKIPPED
staging-parity = SKIPPED
migration-schema = SKIPPED
web = SKIPPED
compose = SKIPPED
FULL_CI = NOT_RUN
```

```text
REAL_PROVIDER_CALL_EXECUTED = false
REAL_CANARY_AUTHORIZATION_CREATED = false
AUTO_MERGE_EXECUTED = false
```

The report byte hash is stored in `W2_T00_SAFE_R1_R5_20260731.md.sha256`.
