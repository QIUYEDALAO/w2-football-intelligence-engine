# W2 T00-SAFE Wave 1 final evidence-remediation report — 2026-08-01

## Boundary and reproduction

```text
EXACT_BASE_SHA = dbc8e1e8aa74a7613fd7121bf6026890c3ee06c6
SCAN_STRATEGY = AST_FIRST_WITH_EXPLICIT_PRIMITIVE_REGISTRY
PR450_REVIEW_HEAD = 360931d7d84bcbe1416c7946992b5218b759fc8a
```

```bash
python3 scripts/audit_t00.py safe --remote github-w2 \
  --pr450-ref refs/remotes/github-w2/pull/450/head --compact
```

The compact SAFE output SHA-256 is
`6aa4e7f3b58dc53f64819b9447148b8fb50e2be3a0b93de256768bdfb4b93436`.
This report proposes classifications and tests; it records no independent
acceptance and implements no runtime change.

## Complete R2 handler denominator

R2 enumerates every `ast.ExceptHandler` in the exact-base Python tree, including
handlers that appear to terminate. Its control-flow walk excludes raises inside
nested functions/classes/lambdas, and its conservative all-path test distinguishes
conditional raise plus fallthrough from terminal raise.

```text
R2_HANDLER_DENOMINATOR = 441
ALL_PATHS_HANDLER_CONTROL_FLOW_TERMINATES = 382
HANDLER_MAY_FALLTHROUGH = 59
```

| handler action | count |
|---|---:|
| `RAISE_TERMINAL` | 82 |
| `CONDITIONAL_RAISE_WITH_FALLTHROUGH` | 8 |
| `RETURN` | 254 |
| `CONTINUE` | 43 |
| `PASS` | 6 |
| `ROLLBACK_THEN_CONTINUE` | 4 |
| `DIAGNOSTIC_THEN_CONTINUE` | 2 |
| `CALL_ONLY_THEN_CONTINUE` | 4 |
| `STATE_UPDATE_THEN_CONTINUE` | 38 |

These 441 rows are a denominator, not 441 defects. Contract tests freeze
conditional-raise/fallthrough and nested-function-raise behavior.

## R3 side-effect and I/O denominator

The explicit registry covers Provider/network transport, DB transaction/write,
file write/replace/delete, object-store write/delete, and message publish/send
primitives. It includes the real Provider call:

```text
src/w2/providers/api_football.py@dbc8e1e8...:139
urllib.request.urlopen -> NETWORK_TRANSPORT
```

```text
R3_SIDE_EFFECT_DENOMINATOR = 837
UNCLASSIFIED_IO_PRIMITIVES = 429
```

| operation | count |
|---|---:|
| `NETWORK_TRANSPORT` | 74 |
| `DB_COMMIT` | 49 |
| `DB_EXECUTE` | 30 |
| `DB_FLUSH` | 16 |
| `DB_WRITE_STAGE` | 49 |
| `DB_ROLLBACK` | 31 |
| `DB_READ` | 23 |
| `FILE_WRITE` | 120 |
| `FILE_ATOMIC_WRITE` | 11 |
| `FILE_REPLACE` | 5 |
| `UNCLASSIFIED_IO_PRIMITIVE` | 429 |

Calls that look like persistence, request, save, write, publish, or transport
but do not match an explicit primitive remain `UNCLASSIFIED`; they are not
silently converted to safe or accepted dispositions.

## Candidate-ID call edges and Gate proposals

The scanner builds a static call-edge manifest rooted at
`scripts/run_prematch_refresh.py:main`. It resolves imports, same-module calls,
`self` calls, and constructor-method calls, and records two explicit dynamic
dispatch proposals for the default Provider client and request ledger. The
full scan discovers 39,178 call sites; 993 rooted edges cover 118 proposed
reachable functions.

Every R2/R3 row records a unique candidate ID, candidate-level edge IDs/chain,
exact-ID blocker mapping, and unresolved reachability where static resolution
cannot prove the edge. No function whitelist is used as final reachability.

All keyword-based or implementer-derived placements are proposals:

```text
TOTAL_R2_R3_CANDIDATES = 1278
PROPOSED_GATE_A = 30
PROPOSED_GATE_B = 96
PROPOSED_GATE_C = 423
PROPOSED_GATE_D = 36
PROPOSED_SAFE_DEGRADATION = 74
PROPOSED_ACCEPTED_WITH_REASON = 619
FINAL_GATE_A = 0
MAPPED_TO_C1_C11 = 35
NEW_FINDING_IDS = 0
INDEPENDENT_REVIEW_PENDING = 1278
```

All 35 blocker mappings are keyed by exact candidate ID. Every candidate has
`accepted_by_independent_reviewer = false`; S07.8 remains open.

## Proposed test contracts

The 30 former Gate-A templates are now `PROPOSED_TEST_CONTRACT`. Each includes
an exact base path/line failure stage and per-delta evidence. Only checkpoint
claim validation has source evidence for provider/business deltas of zero.
Other provider, business, or durable-evidence deltas remain `PENDING_REVIEW`
where prior calls/writes or unimplemented fault injection prevent an exact
claim. No uniform `1/0/1` template remains.

```text
PROPOSED_TEST_CONTRACTS = 30
INDEPENDENTLY_ACCEPTED_TEST_CONTRACTS = 0
INCOMPLETE_PROPOSED_TEST_CONTRACTS = 0
```

## `3420714d` evidence state

```text
SCRIPT_MATRIX_FIELDS = 1160
SCRIPT_MATRIX_EVIDENCE_ATTACHED = 1160
INDEPENDENTLY_VERIFIED_FIELDS = 0
PENDING_INDEPENDENT_REVIEW_FIELDS = 1160
```

Evidence attachment is not semantic verification. Codex does not sign G10.7.

## Corrected PR #450 guard matrix

The scanner parses the trusted main, PR #450 head, and current PR #458 head
only through exact Git objects. It never reads the unchecked delivery-test
worktree. PR #458 does not modify that file and claims no restoration.

```text
PR450_EXACT_EQUIVALENTS = 15
LOST_IN_PR450 = 145
RETAINED_ON_TRUSTED_MAIN = 145
PR450_REPAIR_REQUIRED_GUARDS = 145
UNCLASSIFIED_REMOVED_GUARDS = 0
```

Repair remains on PR #450 via its own rebase/commit. No guard was deleted,
skipped, xfailed, or relaxed by PR #458.

## Other inventories

```text
R1_CONFIGURATION_CANDIDATES = 67
R4_CONCURRENCY_CANDIDATES = 43
R5_COMPUTATION_SYMBOLS = 286
TOTAL_CLASSIFIED_OR_PENDING_AST_CANDIDATES = 1674
UNCLASSIFIED_FINDINGS = 429
UNCLASSIFIED_COMPUTATION_AUTHORITIES = 0
```

The 429 unclassified findings are exactly the unclassified I/O primitives.
Computation authorities remain split into 76 explicit concept/hash domains.
Migration inventory still includes `table.create/table.drop`; metadata coupling
remains Gate D and Gate A retains only manual migration-head preflight.

## Validation boundary

Portable `github-w2`, missing-ref, R2 control flow, R3 primitive, candidate-edge,
Ruff, Mypy, unit/contract, credential-pattern scan, and exact-head path-aware-CI results are
recorded in PR #458 and Issues #454/#455 after the report commit.

```text
FULL_CI = NOT_RUN
REAL_PROVIDER_CALL_EXECUTED = false
REAL_CANARY_AUTHORIZATION_CREATED = false
AUTO_MERGE_EXECUTED = false
```

The report byte hash is stored in the adjacent `.sha256` sidecar.
