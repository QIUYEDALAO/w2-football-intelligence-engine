# W2 T00-SAFE R1–R5 report — 2026-07-31

## Boundary and method

```text
EXACT_BASE_SHA = dbc8e1e8aa74a7613fd7121bf6026890c3ee06c6
SCAN_STRATEGY = AST_FIRST_WITH_TEXT_FALLBACK
PR450_REVIEW_HEAD = 360931d7d84bcbe1416c7946992b5218b759fc8a
```

Run after fetching all refs and the PR #450 head:

```bash
python3 scripts/audit_t00.py safe --compact
```

The scanner reads Python, migration, storage, and computation sources directly
from the exact trusted Git tree, not from the working copy. Its only working-ref
input is the SHA-pinned PR #450 comparison target. The scanner fails if
`origin/main`, base ancestry, or that PR head differs from the recorded SHA.

The compact scanner output SHA-256 is
`cab98eb8c29f21469730340e84b7fbeb15519ac9787c58cf3746f0b36c2d1d18`.

## R1–R5 inventory

| area | AST-first inventory | classification rule |
|---|---|---|
| R1 configuration/defaults | environment and settings reads | every candidate is recorded as a config-read review candidate |
| R2 error/rollback | broad handlers and pass handlers | every candidate is recorded for exception-boundary review |
| R3 transactions/external I/O | provider/client/session request calls and commits | every candidate is recorded as I/O or commit review candidate |
| R4 concurrency/idempotency | lock, unique, idempotency, and `FOR UPDATE` authority files | every authority file is recorded |
| R5 uniqueness | serializer/hash, metric, odds, settlement, probability functions | every candidate symbol has an explicit convergence or distinct-scope classification |

Validated Gate A findings `C1` through `C11` and canonical serialization are
`MUST_FIX_FOR_CANARY`. Fair-odds, market-taxonomy, Brier/ECE, and read-model
authority findings are `MUST_FIX_FOR_CONTINUOUS`. Migration metadata coupling
is `MUST_FIX_FOR_CANARY`. T00 classifies these findings; it does not implement
runtime remediation.

## Storage asset inventory

The exact-base scan finds 59 ORM table authorities and no duplicate
`__tablename__` values. It also inventories 58 Alembic `create_table`, 37
`drop_table`, and zero `rename_table` AST call sites, including dynamic calls.
There are no tracked files under `runtime/` or `reports/`. No data, schema,
migration, runtime, or Provider operation was executed.

The complete table/path/line list and migration call-site counts are emitted by
the scanner. This report intentionally does not claim database-row facts: T00
is a source and provenance inventory, not a live environment authorization.

## Computation authority inventory

The scanner emits every matching symbol with path and line. Each symbol is one
of:

- `MUST_CONVERGE_CANONICAL_SERIALIZATION`;
- `MUST_CONVERGE_EVALUATION_METRIC`;
- `REVIEWED_DISTINCT_ODDS_SCOPE`;
- `REVIEWED_DISTINCT_DOMAIN_SCOPE`.

This inventory identifies serializer/hash proliferation without choosing
`ensure_ascii=True` or `ensure_ascii=False`. No canonical serializer was
implemented and no historical hash was overwritten.

## PR #450 removed-guard equivalence

The AST comparison finds one removed test function and 144 removed assertions,
for 145 matrix rows total. For every row the scanner records:

```text
ORIGINAL_GUARD = test identity or SHA-256 of normalized AST
CURRENT_EQUIVALENT = exact trusted-base file and line
CLASSIFICATION = RETAINED_EQUIVALENT
EVIDENCE = T00 branch retains the trusted-base guard unchanged
```

The original guard expression is represented by normalized-AST SHA-256 to
avoid republishing sensitive literals. PR #450's historical-PR-range
non-authority guard and obsolete-staging-address absence guard both remain
covered: exact-equivalent assertions are detected across test-function moves,
while non-exact removed assertions are retained unchanged on this branch.
No historical guard was deleted, skipped, xfailed, or relaxed.

```text
UNCLASSIFIED_REMOVED_GUARDS = 0
```

## Finding disposition

```text
UNCLASSIFIED_FINDINGS = 0
UNCLASSIFIED_COMPUTATION_AUTHORITIES = 0
UNCLASSIFIED_REMOVED_GUARDS = 0
REAL_PROVIDER_CALL_EXECUTED = false
REAL_CANARY_AUTHORIZATION_CREATED = false
AUTO_MERGE_EXECUTED = false
```

The report byte hash is stored in `W2_T00_SAFE_R1_R5_20260731.md.sha256`.
