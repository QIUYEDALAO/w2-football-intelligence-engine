# W2 T00-GOV Wave 1 grouped-review finalization report — 2026-08-01

## Authority and boundary

```text
EXECUTION_AUTHORITY = GitHub Issue #454 v5 plus latest Wave-1 decision
GOVERNANCE_INCIDENT_AUTHORITY = GitHub Issue #455
EXACT_BASE_SHA = dbc8e1e8aa74a7613fd7121bf6026890c3ee06c6
PR_453 = QUARANTINED_DO_NOT_MERGE
MAIN_CONTAINS_E875050F_PRODUCTION_COMMIT = false
MAIN_PRODUCTION_CODE_AUTOMATION_COMMITS = 0
```

This report covers T00 evidence only. It authorizes no Provider access, canary,
scheduler, deployment, merge, serializer, C9, or runtime remediation.

## Portable reproduction

```bash
git fetch --all --prune --tags
git fetch github-w2 refs/pull/450/head:refs/remotes/github-w2/pull/450/head
python3 scripts/audit_t00.py gov --remote github-w2 --compact
python3 scripts/audit_t00.py safe --remote github-w2 \
  --pr450-ref refs/remotes/github-w2/pull/450/head --compact
```

The remote, main ref, base SHA, PR ref, and PR head are configurable. A missing
PR object exits without a traceback and prints the exact fetch command. The
current-repository compact GOV output SHA-256 is
`acd6200b2b8d0ec04eb435db7fd535fcc45b542eef51a6970625906b859c74ce`.

## Workflow/run/commit closure

The all-ref scan finds nine potentially write-capable workflow paths, 13
relevant workflow runs, and two automation-authored commits. The two confirmed
mutation runs remain:

| run | resulting commit | disposition |
|---:|---|---|
| 30619424000 | `e875050f6bc0286aed389aadfce1e17b2063635a` | quarantined; not on main |
| 30107134502 | `3420714df428d10f441bbc6f011566a42b2fb538` | governance documentation on main |

```text
UNCLASSIFIED_WRITE_CAPABLE_WORKFLOWS = 0
UNCLASSIFIED_WORKFLOW_RUNS = 0
UNCLASSIFIED_AUTOMATION_COMMITS = 0
UNEXPLAINED_BRANCH_MUTATIONS = 0
UNCLASSIFIED_E875_HUNKS = 0
```

All 17 `e875050f` hunks retain explicit `REJECT`, `RETAIN_REIMPLEMENT`, or
`REQUIRES_NEW_DESIGN` dispositions. PR #458 copies none of that implementation.

## `3420714d` field evidence

The scanner emits all eight declared fields for each of 145 script-matrix rows:

```text
path
caller
transitive_chain
environment
deployment_reference
runbook_reference
decision
evidence
```

The validator attaches exact checklist, source, caller, test, deployment,
runbook, or Git evidence where available. It does not claim that generic or
non-empty evidence semantically verifies a field. Codex therefore uses only
`EVIDENCE_ATTACHED_PENDING_INDEPENDENT_REVIEW`; it never emits
`IMPLEMENTER_VERIFIED` or `INDEPENDENTLY_VERIFIED`.

```text
SCRIPT_MATRIX_ROWS = 145
SCRIPT_MATRIX_FIELDS = 1160
SCRIPT_MATRIX_EVIDENCE_ATTACHED = 1160
INDEPENDENTLY_VERIFIED_FIELDS = 0
PENDING_INDEPENDENT_REVIEW_FIELDS = 1160
CONFLICTING_FIELDS = 0
UNREVIEWED_MAIN_AUTOMATION_HUNKS = 145
MAIN_AUTOMATION_DOCS_COMMITS_RECONCILED = false
```

G10.7 remains open. Only an independent reviewer may change the independently
verified count or reconcile the main automation documentation commit.

## G10.7 grouped field-review bundle

The 1,160 fields are assigned exactly once to eight deterministic validation
rules, one for each declared field type. A rule records its algorithm, evidence
source, covered field IDs and exception field IDs. Rule coverage is implementer
evidence only; it does not set an independent acceptance flag.

```text
FIELD_REVIEW_TOTAL = 1160
FIELD_REVIEW_RULE_GROUPS = 8
FIELD_REVIEW_RULE_COVERED = 547
FIELD_REVIEW_EXCEPTIONS = 613
UNASSIGNED_FIELDS = 0
DUPLICATE_FIELD_ASSIGNMENTS = 0
INDEPENDENTLY_VERIFIED_FIELDS = 0
```

The conservative bundle treats transitive-chain, environment and evidence-code
semantics as exceptions. Caller, deployment and runbook fields are rule-covered
only where exact repository references satisfy the field-specific algorithm.
Every rule group and all 613 exceptions remain
`PENDING_INDEPENDENT_REVIEW`; Codex did not sign G10.7.

## SAFE denominator scope carried with this evidence set

The adjacent SAFE report now fails on any unclassified tracked Python root and
records the complete production/exclusion manifest:

```text
INCLUDED_PRODUCTION_ROOTS = apps/, migrations/, scripts/, src/w2/
EXCLUDED_PYTHON_ROOTS = tests/: TEST_ONLY_NOT_PART_OF_PRODUCTION_DENOMINATOR
UNCLASSIFIED_PYTHON_ROOTS = 0
```

This governance report does not independently accept the resulting candidate,
Gate, test-contract, or script-field proposals.

The paired exact-base SAFE evidence accounts for every production `ast.Call`:

```text
TOTAL_PRODUCTION_AST_CALLS = 42054
INTERNAL_PROJECT_CALLS = 10241
KNOWN_PURE_OR_LOCAL_TRANSFORMS = 9590
RECOGNIZED_SIDE_EFFECT_PRIMITIVES = 710
UNRESOLVED_EXTERNAL_CALLS = 6868
UNRESOLVED_DYNAMIC_CALLS = 14645
UNACCOUNTED_PRODUCTION_CALLS = 0
```

Only exact project definitions enter the internal class; only explicit exact
signatures enter the pure/local allowlist. The ledger remains separate from the
grouped decision queue: unresolved calls stay fully visible without creating one
human decision per call site.

## CI evidence boundary

The previous accepted evidence run was path-aware CI, not full CI. The new
denominator-finalization exact-head run is recorded in PR #458 and Issues
#454/#455 after the report commit:

```text
PATH_AWARE_CI = SUCCESS
python-focused = exact-head result recorded externally
images = SUCCESS / images built and pushed / no deployment
CI_REQUIRED = SUCCESS
verify = SKIPPED
predeploy-e2e = SKIPPED
staging-parity = SKIPPED
migration-schema = SKIPPED
web = SKIPPED
compose = SKIPPED
FULL_CI = NOT_RUN
```

The report byte hash is stored in the adjacent `.sha256` sidecar.
