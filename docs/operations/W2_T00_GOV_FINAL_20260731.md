# W2 T00-GOV corrected provenance report — 2026-07-31

## Authority and boundary

```text
EXECUTION_AUTHORITY = GitHub Issue #454 v5 FINAL FROZEN BASELINE
GOVERNANCE_INCIDENT_AUTHORITY = GitHub Issue #455
EXACT_BASE_SHA = dbc8e1e8aa74a7613fd7121bf6026890c3ee06c6
PR_453 = QUARANTINED_DO_NOT_MERGE
MAIN_CONTAINS_E875050F_PRODUCTION_COMMIT = false
MAIN_PRODUCTION_CODE_AUTOMATION_COMMITS = 0
```

This report supersedes the first PR #458 receipt where it overclaimed the
`3420714d` row review and called a path-aware CI run full CI. It authorizes no
Provider access, canary, scheduler, deployment, merge, or later workstream.

## Portable reproduction

The scanner detects the repository remote by URL or accepts an explicit name.
Neither the remote nor the PR ref must be named `origin`.

```bash
git fetch --all --prune --tags
git fetch github-w2 refs/pull/450/head:refs/remotes/github-w2/pull/450/head
python3 scripts/audit_t00.py gov --remote github-w2 --compact
python3 scripts/audit_t00.py safe --remote github-w2 \
  --pr450-ref refs/remotes/github-w2/pull/450/head --compact
```

`--base-sha`, `--main-ref`, `--pr450-ref`, and `--pr450-head` are explicit
overrides. A missing PR object exits without a Python traceback and prints the
exact `git fetch <remote> refs/pull/450/head:<ref>` recovery command.

The corrected compact GOV scanner output SHA-256 is
`9251ebfbccff68f413c4db17e20e93205984e9f193954011b6f3878591bf79b8`.

## Workflow/run/commit closure

The all-ref history scan still finds nine potentially write-capable workflow
paths, 13 relevant workflow runs, and two automation-authored commits.

| mutation run | resulting commit | disposition |
|---:|---|---|
| 30619424000 | `e875050f6bc0286aed389aadfce1e17b2063635a` | quarantined; not on main |
| 30107134502 | `3420714df428d10f441bbc6f011566a42b2fb538` | main governance documentation commit |

The other 11 runs created zero jobs and had no job log or resulting mutation.
The three affected agent heads remain `c2ce6740...`, `7af59a6d...`, and
`9f9c4964...`; branch histories remain linear and attributable.

```text
UNCLASSIFIED_WRITE_CAPABLE_WORKFLOWS = 0
UNCLASSIFIED_WORKFLOW_RUNS = 0
UNCLASSIFIED_AUTOMATION_COMMITS = 0
UNEXPLAINED_BRANCH_MUTATIONS = 0
UNCLASSIFIED_E875_HUNKS = 0
```

## `e875050f` review

All 17 diff hunks retain their independent disposition:

```text
REJECT = 1
RETAIN_REIMPLEMENT = 7
REQUIRES_NEW_DESIGN = 9
UNCLASSIFIED = 0
```

No automation-authored implementation is copied by PR #458.

## `3420714d` corrected field-level review

The commit contains one workflow-deletion hunk and 145 script-authority rows.
For every row the scanner now records all eight fields separately:

```text
path
caller
transitive_chain
environment
deployment_reference
runbook_reference
decision
evidence
```

Only `path` and `decision` are independently checked against the commit tree.
The other six fields per row are `UNREVIEWED`; path existence is not used to
approve caller, chain, environment, deployment, runbook, or evidence claims.
The workflow deletion itself remains reviewed as retirement of the one-shot
mutator.

```text
SCRIPT_MATRIX_ROWS = 145
UNREVIEWED_SCRIPT_MATRIX_FIELDS = 870
UNREVIEWED_MAIN_AUTOMATION_HUNKS = 145
MAIN_AUTOMATION_DOCS_COMMITS_RECONCILED = false
```

Therefore T00-GOV provenance is reproducible, but Issue #455 final zero is not
claimed until an independent reviewer completes those fields.

## Correct CI evidence for PR #458

Run `30643104266` was path-aware CI, not full CI:

```text
PATH_AWARE_CI = SUCCESS
python-focused = 1939 passed
images = SUCCESS / images built and pushed / no deployment
CI_REQUIRED = SUCCESS
verify = SKIPPED
predeploy-e2e = SKIPPED
staging-parity = SKIPPED
migration-schema = SKIPPED
web = SKIPPED
compose = SKIPPED
FULL_CI = NOT_RUN
```

The corrected exact-head run for this remediation is recorded in the PR and
Issue receipts, not backfilled into this historical run statement.

The report byte hash is stored in `W2_T00_GOV_FINAL_20260731.md.sha256`.
