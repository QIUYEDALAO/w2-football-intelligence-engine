# W2 Lineup Baseline Evidence V1

The baseline remains as-of and rolling: ten prior fixtures only, weights `1.0` for the latest five and `0.6` for the preceding five. It records source fixture IDs, common roles/formations and an artifact hash. Future fixtures are excluded by contract tests.

Result: `LINEUP_BASELINE_PASS`. No live lineup-window canary was available; live status is `WAITING_FOR_REAL_LINEUP_WINDOW`.
