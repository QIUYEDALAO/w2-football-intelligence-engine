# 中超 / 瑞超 xG 可得性与恢复连带面审计

Status: `OWNER_RECOVERY_APPROVAL_REQUIRED`

## 结论

- 统一历史语料口径下，中超 `348/421 = 82.7%`、瑞超 `270/378 = 71.4%`，均高于仍启用的阿甲 `344/841 = 40.9%`。若停用规则仅为 `COMPETITION_DISABLED_NO_XG_COVERAGE`，三者标准不一致。
- `2026-08-24T08:50Z`、在后续“审计阶段 Provider 0”约束下发前执行的固定 5+5 探针显示：瑞超 `5/5` 两队 numeric xG，其中 4 场由旧 null 恢复；中超 `0/5`，两队仍为 null。由此只能判定瑞超旧 null 非永久，中超本次样本仍不可得，不能把两联赛合并恢复。
- 生产审计表没有 08-23 停用记录；停用 commit、运行说明和 blocker 都只写 xG。没有发现盘口、机构覆盖或结算可靠性被记录为停用原因。
- 恢复不是单一 enabled 更新：生产 xG refresh 仍硬编码 11 联赛；null statistics 在当前 release 仍会被误当永久 cache hit；`SKIPPED_POLICY` 是终态，scheduler 不会自行重开。

本轮没有部署或生产数据库写入。范围恢复等待 Owner 逐联赛裁定。

## 最新有界探针

执行前门：未来 60 分钟正式临场档位 `0`。严格 10 次 `fixtures/statistics`，全部 HTTP 200；额度 header 从 `7091` 到 `7082`。探针绕开应用数据库与 Provider ledger，生产写入 `0`。

| 联赛 | 固定成熟样本 | numeric 两队 xG | 判定 |
|---|---:|---:|---|
| allsvenskan | 5 | 5 | 旧 null 非永久；当前样本可恢复 |
| chinese_super_league | 5 | 0 | 当前样本仍不可得 |

瑞超样本：`1494246 / 1494247 / 1494245 / 1494243 / 1494241`。中超样本：`1523242 / 1523239 / 1523240 / 1523238 / 1523224`。全部 kickoff 早于执行时至少 144 小时，排除已知发布滞后窗。

## 是否存在非 xG 停用理由

- `league_readiness_audit` 在两联赛的最新记录均为 `2026-08-20 APPLY_COLLECTION_POLICY collection_enabled=true`；08-23 的手工停用没有审计行。
- commit `c582ace4`、运行 blocker 和 Obsidian 记录均只引用 xG 覆盖 / Provider null。
- 已存评估的 bookmaker count：瑞超 p50 `4`、min/max `0/7`；中超 p50 `3`、`0/9`；阿甲 p50 `4`、`0/7`。这些是描述性读数，不构成新的停用阈值。
- 近期已完赛结果持久化覆盖率：瑞超 `16/16`、中超 `14/14`、阿甲 `27/27`，均为 `100%`。未发现结算可靠性差异可支持停用。

因此当前可复核结论是：停用只因当时的 xG 判断；不存在已记录的第二理由。

## 恢复连带面

1. **持续刷新**：生产 `/usr/local/bin/w2-xg-refresh` 仍是固定 11 联赛字符串，不会自动跟随 `league_season.payload.enabled`。本地草案已改为动态读取启用集合，但未部署。
2. **null 重试**：生产 release `d05ab742` 尚未包含 `cbce6be4 / 56be022d`；旧 null 会继续被 `raw_statistics_fixture_ids()` 当 cache hit。直接重开仍无法保持最新 xG。
3. **计划恢复**：原 blocker 计划总数保持 `1,986`：瑞超 `1,324`、中超 `662`。截至本审计仍未过期的为瑞超 `1,296`、中超 `644`；已过期 `28 / 18` 不应重开。repository 明确把 `SKIPPED_POLICY` 视为终态，普通计划生成不会恢复它们。
4. **战绩口径**：当前全量投影（不应用 enabled 过滤）为 `101` 条推荐、`96` 条已结算、`-10.785` 单位；其中瑞超 `3` 条（已结算 `2`，`-0.170`），中超 `8` 条（已结算 `4`，`-3.000`）。当前启用集合因此为 `90` 条、全部已结算、`-7.615`。两联赛若同时恢复，会重新加入 `11` 条，其中 `5` 条当前待结算。
5. **EV 缺口结论**：交接中的 t 检验本来就基于恢复前的原始 65 注；重新纳入两联赛会恢复该原始历史集合，因此不会推翻那条已成立的历史结论。当前已增长到 96 条的集合是另一快照，不得在本审计中替代原 65 注重算或宣称盈亏改善。

## Owner 待决

- 瑞超：是否批准独立恢复；批准后必须先部署 null 重试与动态 refresh，再受控回补、达到既有 30 日覆盖健康线，最后审计式启用并只重开未过期计划。
- 中超：本次 0/5 仍为 null，不建议与瑞超捆绑恢复。若 Owner 仍考虑恢复，应先裁定是否接受 fail-closed 下的间歇覆盖，或另批更大但预注册的分层探针。

本地分支 `codex/xg-competition-recovery` 已准备未部署草案；Ruff、Mypy `282` 个源文件、全量 `2823 passed / 14 skipped`。测试通过不构成范围恢复批准。
