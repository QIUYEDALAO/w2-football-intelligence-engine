# W2 V3-00 staging 基线冻结

**时间：** 2026-07-19 10:40 UTC（只读）

**代码基线：** `600ddc9fa244ca0014bb82248708b53597a592ab`
**分支：** `codex/w2-v3-00-baseline-audit`

## Release 与就绪状态

| 项目 | 冻结值 |
|---|---|
| staging release / API SHA / Web SHA | `600ddc9fa244ca0014bb82248708b53597a592ab` |
| API image | `sha256:853bb8d5e5fe93eb72c1659ff2a1c073e85133bb539e92812a01a1693e576890` |
| Web image | `sha256:cb3ad5c8b3f39d60caf0df01d375cc8e47970780cf81b33da11caf8ae950cdff` |
| `/health` / `/ready` | HTTP 200 / HTTP 200 |
| Alembic | current=head=`0024_create_lineup_intelligence` |
| 服务 | web、api、worker、scheduler、postgres、redis 均 healthy |
| 容器重启 / 当前 OOM | 全部 0 / false |

## 运行与安全边界

- host：4 vCPU、7.4 GiB RAM；冻结时可用内存约 5.6 GiB。
- RSS：web 5.4 MiB、api 277.4 MiB、worker 292.3 MiB、scheduler 148.6 MiB、postgres 158.6 MiB、redis 6.7 MiB。
- 磁盘：118 GiB，已使用 96 GiB（85%），剩余 18 GiB；这是已记录风险，V3-00 不修复。
- 最近七天存在 4 次历史 API cgroup OOM（受 API 1 GiB limit 影响）；当前 release 运行后未重启、未 OOM。后续 canary 以此为历史风险，不得新增 OOM/restart/exit 137。
- scheduler health=healthy；日志中的 future-refresh 均为 `NO_CHECKPOINT_DUE` 与 `provider_calls=0`，`queued_count=0`，正式推荐和 candidate 均为 false。
- scheduler restart policy 为 `unless-stopped`；`w2-staging.service` active/exited，`w2-staging-watchdog.service` 当前 inactive/dead。该 watchdog 状态是冻结事实，不在 V3-00 改动范围内；后续 canary 必须维持或显式批准其监督策略变化。
- Redis：db1 仅 3 个 Kombu binding；没有待执行业务任务。db2 有 371 个有 TTL 的 result/backend key。

## 数据、ledger 与 cohort

| 只读计数 | 冻结值 |
|---|---:|
| `provider_request_logs` 历史累计 | 787 |
| `odds_observations` | 0 |
| `recommendations` / `recommendation_locks` | 0 / 0 |
| `forward_prediction_lock` / `shadow_strategy_lock` | 0 / 0 |
| `results` / `settlements` | 0 / 0 |
| `read_model_checkpoint` | 122 |

公开 DayView 读取返回 `provider_calls=0`、`db_writes=0`。当次 canonical cohort：validation=23、processed=23、eligible=16、excluded=7、pending=0；命中=11、未中=3、走水=2、作废=0，integrity=PASS。

- canonical performance cohort SHA-256（JSON canonical serialization）：`f48f798e9719b5094ee03d392bb37e730cb012a76e551a75a89e145f889c989a`
- forward ledger performance SHA-256（同一方法）：`059c897141ad0e69907171fe0ae55e4d6d9f5d5288c2065eb8638e3ae93be839`
- 此 hash 仅用于检测后续无意的 cohort 语义变化；不回填、重写或修改历史 ledger。

## 不变量与禁止变化

- public read provider / business DB / ledger 写入增量必须为 0。
- API/Web revision、readiness、cohort partition、CLV subset、锁与队列不得出现未计划变化。
- 不新增因子，不改阈值、方向或 canonical cohort；formal AH/OU、LMM 数值调整、production 与锁单保持关闭。
- 本阶段不部署、不进行 staging canary。任何新增 restart/OOM、queue 增长、SHA mismatch、ready 失败或 cohort hash 变化均为停止条件。

## 本地验收基线

| Gate | 结果 |
|---|---|
| corpus 校验 | 2 passed |
| Ruff | PASS (`uv run ruff check .`) |
| Mypy | PASS（229 source files） |
| 全量 pytest | PASS：1233 passed、4 skipped、1042 warnings（34.00s） |
| pytest skips | 3 个 staging-parity skip（本地无 Docker uid/gid fixture）；1 个 migration skip（未设置 `W2_TEST_POSTGRES_URL`） |
| Web typecheck | PASS |
| Web production build | PASS |
| Playwright | PASS：9 passed（3.6s） |

V3-00 不运行 migration、isolated runtime 或 staging canary；这些不属于“只读冻结”且会改变隔离/运行状态。后续阶段只有在对应 gate 指定时才运行。
