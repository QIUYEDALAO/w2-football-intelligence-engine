# RecommendationDecisionV3 frozen corpus

该 corpus 是 V3-00 的静态语义基线，不是 production fixture，也不会触发 provider、数据库或 ledger 写入。每个场景同时固定 analysis-card、Dashboard card、DayView、decision contract v2、适用的 lifecycle 证据和 performance cohort membership。

后续 V3 阶段必须保留这些状态语义，或在对应 gate 中显式更新基线和审计原因。
