# The image ships with PubkeyAuthentication off, leaving password login as the
# only way in.  Keys are enabled here.  Password login stays on for now so a
# lost key cannot lock the machine out; turn it off once key access is
# confirmed from everywhere that needs it.
PubkeyAuthentication yes
